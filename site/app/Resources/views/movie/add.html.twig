<html>
  <head>
          <title>Ajout d'un film </title>
  </head>
   <body>
      {{ form_start(form) }}
        {{ form_errors(form) }}
          <div>
          {{ form_label(form.name) }}
          {{ form_errors(form.name) }}
          {{ form_widget(form.name) }}
         </div>
         {# ... #}
         {{ form_end(form) }}
  </body>
</html>
